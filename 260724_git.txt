1. 파일 이름 변경하고 싶을 때
- OS의 파일 이름 변경 로직 
-> OS에서 이름 변경 == 경로를 바꿈(위치를 바꿔줌)
-> git의 변경 파일 추적 로직
260722_py.txt(삭제) -> 260722_temp_py.txt(생성)
-> 기존의 파일 삭제 / 이름변경 -> 새로 추가된 파일로 인식
=> add 2개 파일(삭제, 등록) -> Rename
<삭제된 파일도 같이 등록하고 add해줘야 이름 변경됨 (내용이 같을 때)
————————————————
Git add 260722_py.txt
Git add 260722_temp_py.txt

2. 특정 파일의 커밋 메시지 변경하고 싶을 때
-> 무조건 변경사항을 만든다(작게)
-> add, commit, push 
-> 등록한 파일 단위로 커밋 메시지가 기록됨

//크게 보면 둘 다 함수 
내장함수 sorted() -> 자체적으로 쓰임 (괄호안에 list 이런게 들어감)
메서드 .sort() -> 어디에 붙어서 사용함
