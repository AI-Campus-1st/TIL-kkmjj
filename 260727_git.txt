자료형(data type)

내장함수 내장클래스
Type / dict (사용X) -> my_dict, dict_1

숫자형
- int(정수형)
3+7
- float (실수형)3.0+7.3
	*부동소수점 문제 
- Bool (참/거짓)
	비교연산자
	false로 빠지는 애들 - 0, 0.0, ‘’, [], {}, (), None

군집형 (+, *연산자)
-> Sequence(순서가 있다. ordered())

- str(문자열형)  immutable
	‘ python’ / .strip(), .replace(), split(), .find(), 
- list(목록형) -> 파이썬 내부 연산  mutable
	[1, 3, ‘hi’] / .append(), .index(), .pop(), .sort(), .copy()
- Tuple(튜플형) / immutable
	(1, 3, 5)
- Range (숫자 생성기) 
	range(10)

->None-Sequence(순서가 없다. Unordered)
+mutable / iterable
-set (key로만) / 주로 unique한 값만 남길 때
-dict(key:value쌍으로)
*Key: 키 (immutable한 애만 올 수 있다. - hashable)


